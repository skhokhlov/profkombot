version: '3.2'

services:

  db:
    image: postgres:11-alpine
    restart: always
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080

  api:
    build: ./api
    restart: always
    ports:
      - 8080:5000
    environment:
      DATABASE_PASSWORD: ${POSTGRES_PASSWORD}
    depends_on:
      - "db"

  bot:
    build: ./bot
    restart: always
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
    depends_on:
      - "db"
      - "api"

  feedback:
    build: ./feedback
    restart: always
    environment:
      BOT_TOKEN: ${BOT_TOKEN_FEEDBACK}
      ADMIN_ID: ${ADMIN_ID}